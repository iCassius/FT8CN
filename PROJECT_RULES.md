# FT8CN 项目准则

> 本文件是每次修改本项目前必读的工作准则。配合 `doc/HANDOFF.md`（会话交接日志）使用：
> **开工先读这两个文件，收工更新 doc/HANDOFF.md。**

## 一、项目定位

- 本仓库是 BG7YOZ 原版 FT8CN 的**社区维护版**，面向中文 HAM 社区发布，已与上游完全独立，不再跟随上游演进。
- 取舍原则：**稳定性 > 兼容性 > 性能 > 新功能**。要对其他用户的设备负责，不拿社区用户当小白鼠。

## 二、范围控制（防目标偏移）

1. **一次只解决一个问题。** 每个 commit / PR 对应一个明确的问题或功能，禁止"顺手重构"、"顺手格式化"。
   - 教训：v0.93 的"全面架构优化"大提交把呼号前缀查询改成了精确匹配，导致 DXCC/归属地/分区显示全部失效，且数月未被发现。
2. **动核心链路前先写清楚改动理由和影响面**，列出受影响的调用方再动手。
3. 改动应保持**最小 diff**：不要改写无关代码的风格、注释、命名。
4. 新功能默认先做最小可用版本（如崩溃捕获先做本地保存，导出 UI 下个迭代再说）。

## 三、高风险区域地图（动之前多想一步）

| 区域 | 文件/包 | 风险点 |
|------|---------|--------|
| 录音/解码时序 | `wave/HamRecorder`、`ft8listener/` | 录音窗口必须与 UTC 时隙对齐；监听器跨线程增删；任何样本欠账都会跨周期错位 |
| 电台协议 | `icom/`、`rigs/`、`connector/` | UDP 包节拍（20ms）、seq 缓存、PTT 状态机；XieGu 发包线程是常驻线程 |
| 呼号数据库 | `callsign/CallsignDatabase` | callsigns 表存的是 **CTY.DAT 前缀**，查询必须前缀匹配取最长命中；CTY.DAT 经度西经为正，显示时要取反 |
| 自动通联状态机 | `ft8transmit/FT8TransmitSignal` | 消息序号(1-6)状态迁移，改动必须真机走完整 QSO 流程 |
| 后台线程 | `AppExecutors`、各 `new Thread` | timerTrigger 是全局单线程，禁止在 doOnSecTimer 回调里做耗时操作 |
| UI 与后台共享对象 | `ui/WaterfallView` 等 | bitmap 等资源的重建/回收必须与后台绘制线程互斥 |

## 四、关键路径必验清单（每次发版前）

核心使用场景（按用户实际场景确定）：

- [ ] **WiFi 网络模式（ICOM/协谷）**：连接、解码若干周期、自动回复一次完整 QSO
- [ ] **长时间挂机**：至少连续运行 2 小时不崩溃、不卡解码、手机不过热（这是历史问题高发区）
- [ ] **日志/QSL**：通联记录正确入库，ADIF 导出正常；启用 Cloudlog/QRZ 时上传成功
- [ ] 发射时序：解码→解析→PTT→音频发送的时间链路在 2 秒裕量内

无法全部实测时，至少跑通受改动影响的那条路径，并在 HANDOFF.md 里写明哪些没测。

## 五、测试策略

- 自动化测试**从协议/数据层开始**（纯 JVM 可测）：呼号前缀查询、FT8 消息解析/生成、时隙计算、ICOM/XieGu 包格式编解码。
- 原则：每修一个数据层/协议层 bug，补一个能拦住它的单元测试（DXCC 回归本可被一个单测拦住）。
- 解码正确性可用 WAV 样本喂 JNI 验证（需真机或处理 .so 加载），优先级次之；UI 自动化暂不投入。

## 六、发版规范

- 版本号 `主.次.构建号`（如 `0.93.005`），构建号必须是三位；唯一来源是 `ft8cn/gradle.properties` 的 `ft8cn.versionName`/`ft8cn.versionCode`，tag 使用同一版本的 `v` 前缀。已存在的 tag 和 Release 资产不得覆盖。
- 普通推送只同步代码；**打 `v*.*.*` tag 并推送才会触发 GitHub Actions 构建 APK 并发布 Release**：
  ```bash
  git tag -a v0.93.005 -m "FT8CN v0.93.005"
  git push origin v0.93.005
  ```
- 修复类改动在 `fix/*` 分支上做，验证后合入 `release`。
- 发版说明写清：修了什么、用户可感知的变化、已知未解决问题。

## 七、问题反馈与定位

- App 内置崩溃捕获（`FT8CNApplication`）：崩溃堆栈写入 `Android/data/com.bg7yoz.ft8cn/files/crash/`，最多保留 10 份，不联网不上传。让用户通过电脑 USB 或 adb 取出文件反馈。
- 自己定位问题优先用 `adb logcat`；解码类问题关注 `FT8SignalListener`、`HamRecorder`（`GetVoiceData`）的日志时间戳是否对齐时隙边界。

## 八、工作连续性

- 每次会话结束，把"做了什么/为什么/什么状态/下一步"按模板**追加到 `doc/HANDOFF.md` 顶部**（带日期），不覆盖历史。
- 准则本身的修订也走小步快跑：每次只改有共识的条目。
