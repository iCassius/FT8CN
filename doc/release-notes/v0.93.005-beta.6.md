# FT8CN v0.93.005-beta.6 Pre-release Notes

## 测试预发布

此包仅用于 `v0.93.005-beta.6` 的测试预发布。它不是正式版，也不代表真实电台/HIL 已通过。

## 本次阻断收口

- 配置生命周期阻断的根因是测试立即断言异步 `DatabaseOpr.dbExecutor` 结果；`ActivityScenario.launch()` 不等待配置完成。临时 latch 占用共享 `diskIO` 后启动 Activity 可稳定复现 `expected:<1> but was:<0>`，证明是测试等待/隔离问题，不是已证实的 production 生命周期竞态。
- `MainActivityConfigLifecycleTest` 现在通过 `configLoadComplete` observer + bounded latch 等待真实完成事件，并在 finally 移除 observer；初始 Activity 与重建 Activity 的断言均经过明确完成边界。没有 skip、固定 sleep、放宽断言或 production 测试开关。
- 此外保留本 beta 候选已有的 HamRecorder stop→start ownership、真实 MicRecorder control instrumentation、AudioForegroundService production boundary 与 debug-only failure ACK、notes gate 等修复和边界。

## 最终自动与 AVD 验证

- JVM：`:app:testDebugUnitTest --rerun-tasks` 为 21/21，失败 0、错误 0、跳过 0。
- Java/androidTest Java 编译、`assembleDebug`、`packageTestApk`、`lintDebug` 均通过；lint 为 0 errors / 330 warnings。
- release contract 默认与 `--history` 均通过；本地 release gates 为 10/10；`git diff --check` 通过。
- 独占 `Pixel_10_Pro_XL` / API 37 AVD：该 test class 连续 10 轮均 2/2；相关配置回归 12/12；全量 `connectedDebugAndroidTest` 连续 2 轮均 61/61，失败、错误、跳过均为 0。旧 notes/HANDOFF 中未经当前 live run 支持的 14/14 与 61/61 记录已由本证据纠正。
- 最终 debug beta 以 `pm uninstall --user 0` 后干净安装，Monkey 启动和包进程存活通过，crash buffer 与应用 crash 目录未发现 `com.bg7yoz.ft8cn.beta` 记录。

## 包与使用边界

- 包名：`com.bg7yoz.ft8cn.beta`；版本：`0.93.005-beta`；`versionCode`：`93005`；签名：Android Debug。
- APK 文件名模式：`FT8CN-v0.93.005-beta-<short-commit>.apk`；最终路径、大小、SHA-256 和证书指纹以最终构建输出为准。
- 这是独立 beta 包，可与正式包并存；不能代替正式签名发布。请在受控条件下继续验证 PTT、断线恢复、真实录音、长时稳定性、功耗和温升。
- 本次没有真实手机、电台、CAT/PTT/TX、完整 QSO、用户 HIL 或正式签名发布证据；`v0.93.005-beta.6` tag/Release 已存在但保持不可变，`release` 合并/推送和 `formal` 发布未执行。

## 签名身份阻断（保持 beta.6 不可变）

- beta.6 的 Android Debug/CI 签名身份不是可持续的 beta 升级身份；独立取证显示 beta.5、beta.6 CI 与本机身份不同，因此 beta.6 最终为 `NO-GO`，不能承诺覆盖升级。
- beta.6 的 tag/Release 内容保持不可变。本仓库仅补充这份说明，记录已知签名问题，不移动、删除或覆盖 beta.6 产物。
- 用户从 beta.6 迁移到 `v0.93.005-beta.7` 前必须卸载一次 `com.bg7yoz.ft8cn.beta` 并重新安装；beta.7 以后应由同一持久 beta-only certificate 形成覆盖升级链。
- beta-only/debug/non-formal 与 formal gate 完全分离；本 beta 没有真实手机、电台、CAT/PTT/TX 或用户 HIL 证据。
