# FT8CN v0.93.005-beta.6 Pre-release Notes

## 测试预发布

此包仅用于 `v0.93.005-beta.6` 的测试预发布。它不是正式版，也不代表真实电台/HIL 已通过。

## 本次阻断收口

- HamRecorder stop→start ownership 已线性化：stop 先撤销旧 Mic session identity，再在锁外释放 AudioRecord 和前台服务；并发 start 只能取得新的 session id，旧 cleanup 不会取消新录音。
- 新增真实 MicRecorder fake/control 并发 instrumentation，使用 latch 强制 stop 尚未完成 identity retirement 时发起 start，验证异常传播、新旧 session id 不同、新 session 真正运行并交付数据。
- 移除 AudioForegroundService production Intent 测试 extra 和 synthetic post-startForeground throw；失败 ACK 由 debug-only 独立 Service subclass 在真实 promotion boundary 注入，release manifest 不含测试 Service 或测试开关。
- beta notes 现在由本地 release gates 强制检查存在、非空和边界内容，workflow 对动态 notes 文件执行非空检查。

## 最终自动与 AVD 验证

- JVM：`:app:testDebugUnitTest --rerun-tasks` 为 21/21，失败 0、错误 0、跳过 0。
- Java/androidTest Java 编译、`assembleDebug`、`packageTestApk`、`lintDebug` 均通过；lint 为 0 errors / 330 warnings。
- release contract 默认与 `--history` 均通过；本地 release gates 为 10/10；`git diff --check` 通过。
- 独占 `Pixel_10_Pro_XL` / API 37 AVD：受影响目标集合 14/14 连续 5 轮；全量 `connectedDebugAndroidTest` 61/61 连续 2 轮；失败、错误、跳过均为 0。
- 最终 debug beta clean uninstall/install、Monkey 启动和包进程存活通过，crash buffer 未发现 `com.bg7yoz.ft8cn.beta` 记录。

## 包与使用边界

- 包名：`com.bg7yoz.ft8cn.beta`；版本：`0.93.005-beta`；`versionCode`：`93005`；签名：Android Debug。
- APK 文件名模式：`FT8CN-v0.93.005-beta-<short-commit>.apk`；最终路径、大小、SHA-256 和证书指纹以最终构建输出为准。
- 这是独立 beta 包，可与正式包并存；不能代替正式签名发布。请在受控条件下继续验证 PTT、断线恢复、真实录音、长时稳定性、功耗和温升。
- 本次没有真实手机、电台、CAT/PTT/TX、完整 QSO、用户 HIL 或正式签名发布证据；`v0.93.005-beta.6` tag/Release、`release` 合并/推送和 `formal` 发布均未执行。
