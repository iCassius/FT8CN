# FT8CN v0.93.005 Release Notes

## 发布状态：正式发布仍阻断

本文件描述 `v0.93.005` 的正式候选源码与自动验证边界，不表示正式签名、tag 或 GitHub Release 已完成。正式发布必须先完成签名迁移确认；在用户明确选择并提供新的长期正式 keystore、可信证书 SHA-256 指纹和发布批准前，tag workflow 会 fail-fast，不创建正式 Release。

## 功能与稳定性改进

- 配置加载改为明确的“逐项读取 + 完成通知”状态模型；旋转、重建和损坏配置值不会把未完成加载当作可用状态。
- 配置生命周期回归测试改为等待真实 `configLoadComplete` observable 事件：确定性占用共享 `diskIO` 可证明 Activity launch 不等于异步配置完成，修复了测试时序假设；没有修改 production 生命周期或留下外部可控测试开关。
- 发射流程由统一 PTT/SCO 协调器管理，断开连接、停止和延迟回调会安全收尾，避免生命周期切换中重复或遗漏恢复动作。
- 网络电台与音频任务使用有界串行队列、提交快照和会话 generation 隔离；TCP/UDP EOF、断线与旧回调不会污染新的连接状态。
- FT8 解码改为单飞协调和 epoch 生命周期隔离；重叠时隙跳过，空结果、异常和取消都有统一终态，旧解码结果不能污染新周期。
- 修复解码回调在入口 epoch 检查后被 `onCleared()`/停止插入的竞态：消息列表/UI、关注列表、QTH、自动回复解析和数据库持久化现在都要通过短门控准入；停止先关闭门控，再取消可能仍在返回的解码任务。
- 修复解码 Future 在真正启动前被取消时残留 `active=true` 的问题；运行中的旧 Future 不会清理新任务状态。
- 修复解码耗时发布在取消检查后仍可更新 `timeSec`/`decodeTimeSec` 的生命周期竞态；耗时 UI 现在也必须通过当前 epoch 门控准入。
- 录音启动改为 session/generation 隔离的异步初始化；权限、`AudioRecord` 构造/启动异常和停止都统一清理各自服务与资源，旧 session 不会误停新 session。
- 收口录音会话交付竞态：`MicRecorder` 为每个 session 捕获独立回调和浮点缓冲，并以 session identity/lease 在交付点准入；`HamRecorder` 将 monitor 绑定 recorder generation，停止和清理走单一终态，旧音频、强制结算和解码回调不能进入新 session。
- 前台录音服务增加严格 session/startId 状态机和 `ResultReceiver` ACK；stale STOP 不会误停新 session，无 active session 时可靠自停，`startForeground` 失败会清理服务并向启动方返回失败真值。
- `MainViewModel` 与 `UtcTimer` 的 heartbeat 使用 cleared/epoch fence；补充正常 duration publication、heartbeat、服务生命周期、录音交错和 monitor 异常传播测试。
- 新增真实逻辑层录音失败清理 instrumentation，并用 `Future`、executor、latch 强制交错验证解码门控和旧 `finally` 不清理新任务。
- 录音启动真值、Yaesu/FT-710 生命周期、Wavelog station ID 精确匹配和 JTDX priority 前置计算已修复；异步归属地刷新不重复计算 priority/会话统计。
- 保持 Android `minSdk 23`，修复发射任务集合的 API 兼容性；补齐希腊语、日语和西班牙语的新增界面字符串。
- 保留本地 TXT/ADIF `file/content` 导入 intent filter，并仅对该非 Web filter 做精准 `AppLinkUrlError` suppression。

## AUTO_VERIFIED 自动验证状态

- 最终代码通过全量 `:app:testDebugUnitTest --rerun-tasks`：21/21，失败 0、错误 0、跳过 0；新增测试覆盖 duration admitted、heartbeat fence、服务 stale/matching STOP、promotion boundary 失败 ACK、Mic stop→start ownership、Ham monitor generation、异常传播和 Decode executor 幂等。
- `:app:compileDebugJavaWithJavac`、`:app:compileDebugAndroidTestJavaWithJavac`、`assembleDebug`、`packageTestApk`、`lintDebug` 均通过；lint 为 0 errors / 330 warnings；最终 beta APK metadata/SHA/证书以最终会话输出为准。
- 发布门禁已通过：`check_release_contract.py` 默认与 `--history` 均通过；`python -m unittest scripts.test_release_gates -v` 为 10/10；`git diff --check` 通过。

## AVD_VERIFIED 与发布边界

- 已确认无实体设备、无其他 emulator/qemu owner，独占唯一 `Pixel_10_Pro_XL` / API 37 AVD；配置生命周期 test class 连续 10 轮均 2/2，相关配置回归 12/12，全量 `connectedDebugAndroidTest` 连续 2 轮均 61/61，失败/错误/跳过均为 0；最终 debug beta clean uninstall/install、Monkey 启动、包进程存活且 crash buffer 与应用 crash 目录无该包记录。旧的 14/14 与 61/61 记录已被 live rerun 发现的第 75 行时序失败 supersede，不再计入本候选证据。
- 没有真实手机、电台、CAT/PTT/TX、完整 QSO、长时稳定性、功耗/温升或用户 HIL 证据。当前 beta candidate：`GO` 仅限 AUTO/AVD/clean beta smoke，不能替代 HIL；`v0.93.005-beta.6`：`NO-GO`（已有 tag/Release 但签名身份不可持续，内容保持不可变）；`release`：`NO-GO`（未合并/推送）；`formal`：`NO-GO`（无长期签名材料、可信证书指纹和明确批准，未生成正式签名 APK）。
- AUTO_VERIFIED 不等同于实机/HIL，也不构成正式发布授权。

## 签名迁移说明

- 已公布的 `v0.93.004` APK 使用 Android Debug 证书；本仓库没有证明其对应私钥可恢复的证据，也不假设旧私钥可取回。
- Android 同包升级要求签名证书一致。任何新 formal key 都不能覆盖已安装的 `com.bg7yoz.ft8cn` v0.93.004；即使 `versionCode` 更高，也会被签名校验拒绝。
- 如果旧私钥不可恢复，用户仍需明确选择是否采用“一次性签名迁移”，并确认卸载旧包后干净安装的方案。真实 Android 设备的迁移、数据备份和安装结果不属于本候选已完成的证据；操作前应备份 ADIF/QSO 和可导出的配置，当前没有承诺自动迁移应用私有数据。
- beta.5/beta.6 曾使用不同的非持久 debug/CI 身份；从 `v0.93.005-beta.7` 起，测试包 `com.bg7yoz.ft8cn.beta` 改用独立、持久的 beta-only certificate。beta.5/beta.6 用户需要先卸载一次，beta.7 以后才形成可覆盖升级链。
- beta-only 与本地普通 debug 仍是不同语义：普通开发 `debug` 可继续使用 Android Debug 证书；tag prerelease 必须使用 beta-only material。两者都不是正式版替代品，formal 签名与 gate 完全独立。

## 版本与产物

- canonical version: `0.93.005`
- `versionCode`: `93005`
- planned tag: `v0.93.005`；正式 workflow 只接受与 `origin/release` 精确相同的 SHA，并拒绝覆盖任何已存在的同名远端 tag 或 GitHub Release
- TEST/BETA: `FT8CN-v0.93.005-beta-<short-commit>.apk`
- formal candidate: `FT8CN-v0.93.005-release-<short-commit>.apk`；本会话未生成或分发正式签名 APK

完整历史记录见 [`doc/RELEASES.md`](../RELEASES.md)；本文件才是该版本的 Release notes body。
